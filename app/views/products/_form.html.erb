<%= form_for(@ap_product) do |f| %>
  <% if @ap_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ap_product.errors.count, "error") %> prohibited this ap_product from being saved:</h2>

      <ul>
      <% @ap_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :naziv %><br>
    <%= f.text_field :name , :class => "form-control"%>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
