<%= form_for(@primary_assortment) do |f| %>
  <% if @primary_assortment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@primary_assortment.errors.count, "error") %> prohibited this primary_assortment from being saved:</h2>

      <ul>
      <% @primary_assortment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :naziv %><br>
    <%= f.text_field :name, :class => 'form-control'%>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Spasi"%>
  </div>
<% end %>
